# Free stateless API. History and images use the existing Firestore, not local disk.
services:
  - type: web
    name: daytrade-judgment-api
    runtime: docker
    plan: free
    region: singapore
    numInstances: 1
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeployTrigger: off
    healthCheckPath: /healthz
