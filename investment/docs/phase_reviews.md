# スイングVertical Slice 実装レビュー
対象：正本17文書。正本は変更しない。実装上の初期値・制限は別に記録する。

## Phase 1 基盤
1. 実装：プロジェクト、config、Evidence/Bundle/警告/スコア/判断モデル、SQLite履歴、Provider契約、CSV検証、Preflight、ログ。
2. テスト：基盤7件成功。欠損、銘柄不一致、未来情報、競合、価格調整、独立確認、配点契約。
3. 仕様対応：10のデータ・Provider契約、06/07の欠損・警告、14の初期設定。
4. 技術的負債：金融・ニュースの自動取込は未接続。正規化した資料を補足JSONで手動入力。
5. 未解決：SQLiteの分析保存・承認の結合検証はPhase 3で実施。
6. 次Phase：基盤テスト合格のため進行可能。外部通信・課金・注文はなし。

## Phase 2 分析エンジン
1. 実装：確定日足/週足、日週MA、BB、RSI、ATR、Fib、確定pivot、窓、支持抵抗クラスタ、出来高、損切先行の価格プラン。
2. テスト：基盤を含む14件成功。木曜と金曜引け後の週足境界を別検証。初回の不合格はテストの曜日設定を修正。
3. 仕様対応：05、14。損切は支持帯とATRバッファで先に決定。利確幅/RRから逆算しない。
4. 技術的負債：実測価格別出来高は出所付きの価格帯入力に対応。取引明細からの分布生成は未実装。
5. 未解決：実市場の呼値表と取引カレンダーは利用者の検証済み入力。未設定は価格/営業日数を未算出にする。
6. 次Phase：境界・構造計算テスト合格、Phase 3へ進行可能。

## Phase 3 スイング判定
1. 実装：32カードの検証、固定配点、週足方向の数値判定、マクロ経路、価格別Entry採点、Decision、例外・Severe承認履歴。
2. テスト：Phase 1〜4の結合段階で39件成功。Critical/Severe/Warning、欠損、低RR、上値7%未満、承認・失効・拒否を含む。
3. 仕様対応：02/07/10/11/13。元スコアを保存し、判断上書きの前後・理由・Evidenceを保存。承認は別レコード。
4. 技術的負債：財務・信用・比較のカードは根拠付き手動入力。外部AIと人間比較は未実施。
5. 未解決：実資料に基づく採点意味の検証は次段階。自由採点や数値推測で埋めていない。
6. 次Phase：自動テストで重要不変条件を確認できたため進行可能。

## Phase 4 UI
1. 実装：結論・投資妙味・Entry品質・全価格欄・RR・決算・理由・懸念・撤退・信頼度・Evidence・シナリオ・履歴。
2. テスト：AppTestで架空銘柄の「分析する」を実操作し、例外なし・必須表示・保存結果を確認。
3. 仕様対応：ユーザー指定の18表示項目、07/10の警告とユーザー承認表示。
4. 技術的負債：高機能チャート操作と手動カード個別フォームは未対応。補足JSONで入力。
5. 未解決：実ブラウザでの起動確認は最終確認に実施。履歴表示は現在の推奨と区別する。
6. 次Phase：画面の自動動作確認後、共通入力・架空データで最終検証へ進行可能。

## Phase 5 共通入力・最終確認
1. 実装：銘柄手入力、共通CSV、補足JSON、架空日足450本・根拠・採点カード、操作説明。
2. テスト：UTF-8・銘柄・時間・価格・調整・欠損・重複・メタ情報の検証。最終実行結果は実装結果報告書に記録。
3. 仕様対応：06/10の無料・手動入力優先。外部Providerとカード評価の接続口を分離。
4. 技術的負債：SBI固有形式、画像抽出、公式カレンダー・呼値表の自動取込は次段階。
5. 未解決：実銘柄比較・全66受入・中長期/デイトレは未完了。今回の試用版の完了と区別。
6. 次Phase：最終テストと実起動を確認後、今回指定の区切りとして報告する。実データ・AI接続へ自動で範囲を広げない。

## セルフレビューで修正した点
- NumPy真偽値のJSON保存不整合を修正。
- 前日の高値超えだけでは抵抗帯ブレイクにせず、実際の帯上端との比較へ修正。
- Warningが通常買いのまま残る経路を条件付きへ修正。
- 一回の高安変化を最高アンカーにせず、複数の確定高安を確認。
- 支持の10点には複数系統を要求。未確認マクロを中立点にしない。
- 信頼度を任意の平均値にせず、必要カードの根拠・日時・出所・照合で計算。
- 資料鮮度が未確認なら、計算済みスコアも暫定とし正式買いを抑制。
- 入力・config・期限変更による承認失効、拒否とAI判断の分離を確認。

## 最終確認結果
2026-09-10、構文確認成功、全43テスト成功（38.42秒）。HTTP health/rootとも200。
架空1銘柄を実際のローカルSQLiteへ保存し、保存履歴画面の操作テストにも成功。
正本17文書はハッシュ一致で未変更。今回指定の到達点を満たしたため、ここで結果報告とする。
